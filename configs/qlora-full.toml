model_name = "google/gemma-4-E4B-it"
train_file = "data/train.jsonl"
output_dir = "outputs/gemma4-e4b-qlora-full"
max_length = 512
# 80 examples / (batch 1 * gradient accumulation 4) = 20 steps for one epoch.
max_steps = 20
per_device_train_batch_size = 1
gradient_accumulation_steps = 4
learning_rate = 0.0002
lora_rank = 8
lora_alpha = 8
seed = 3407
